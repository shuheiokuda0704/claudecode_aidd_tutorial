# Test Environment Configuration
NODE_ENV=test

# Server Configuration
PORT=3001

# Database Configuration (using the same database for now, could use a separate test DB)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/todo_db

# Logging
LOG_LEVEL=error

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
